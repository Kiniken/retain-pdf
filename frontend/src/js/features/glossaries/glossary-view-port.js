import {
  appendGlossaryEntryRow,
  bindGlossaryViewEvents,
  clearGlossaryCsvText,
  closeGlossaryDialogView,
  openGlossaryDialogView,
  readGlossaryCsvText,
  readGlossaryEditorPayload,
  renderGlossaryEditor,
  renderGlossaryList,
  setGlossaryImportVisible,
  setGlossaryStatus,
} from "./view.js";

export function createGlossaryViewPort({
  addEntryRow = appendGlossaryEntryRow,
  appendGlossaryEntryRow: legacyAddEntryRow,
  bindEvents = bindGlossaryViewEvents,
  bindGlossaryViewEvents: legacyBindEvents,
  clearCsvText = clearGlossaryCsvText,
  clearGlossaryCsvText: legacyClearCsvText,
  closeDialog = closeGlossaryDialogView,
  closeGlossaryDialogView: legacyCloseDialog,
  openDialog = openGlossaryDialogView,
  openGlossaryDialogView: legacyOpenDialog,
  readCsvText = readGlossaryCsvText,
  readGlossaryCsvText: legacyReadCsvText,
  readEditorPayload = readGlossaryEditorPayload,
  readGlossaryEditorPayload: legacyReadEditorPayload,
  renderEditor = renderGlossaryEditor,
  renderGlossaryEditor: legacyRenderEditor,
  renderList = renderGlossaryList,
  renderGlossaryList: legacyRenderList,
  setImportVisible = setGlossaryImportVisible,
  setGlossaryImportVisible: legacySetImportVisible,
  setStatus = setGlossaryStatus,
  setGlossaryStatus: legacySetStatus,
} = {}) {
  return {
    addEntryRow: legacyAddEntryRow || addEntryRow,
    bindEvents: legacyBindEvents || bindEvents,
    clearCsvText: legacyClearCsvText || clearCsvText,
    closeDialog: legacyCloseDialog || closeDialog,
    openDialog: legacyOpenDialog || openDialog,
    readCsvText: legacyReadCsvText || readCsvText,
    readEditorPayload: legacyReadEditorPayload || readEditorPayload,
    renderEditor: legacyRenderEditor || renderEditor,
    renderList: legacyRenderList || renderList,
    setImportVisible: legacySetImportVisible || setImportVisible,
    setStatus: legacySetStatus || setStatus,
  };
}
